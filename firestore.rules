
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any user to read/write all documents for debugging.
    // WARNING: This is insecure and should not be used in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
